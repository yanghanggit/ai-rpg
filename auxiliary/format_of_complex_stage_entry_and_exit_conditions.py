# # 特殊的格式字符串
# from typing import List

# #
# def is_complex_stage_condition(propname: str) -> bool:
#     #下面是例子：输入 = “(内容A|内容B)” 。如果输入符合这种格式，那么就是复杂条件。否则就不是
#     return propname.startswith("(") and propname.endswith(")") and "|" in propname

# #
# def parse_complex_stage_condition(propname: str) -> List[str]:
#     # 下面是例子：输入 = “(内容A|内容B)” 。如果输入符合这种格式，那么就是复杂条件。否则就不是
#     if is_complex_stage_condition(propname):
#         return propname[1:-1].split("|")
#     return []





        